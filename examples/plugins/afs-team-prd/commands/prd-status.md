---
description: Summarize the current team PRD — inflight work, recent done items, open questions
---

Summarize the current state of the `team-prd` MCP workspace.

1. Read `/prd.md` — show the top 5 goals/sections.
2. List `/inflight/` with `depth: 2`. Group by developer handle (subdir name).
   For each inflight file, show: owner, slug, and one-line description pulled
   from the file's first heading or first prose line.
3. List the 5 most recently-modified entries in `/done/` (by `modified_at`).
4. Read `/questions.md`. Show only entries whose marker contains
   `STATUS: open`.
5. End with a one-line summary: "N inflight across M devs, K open questions."
